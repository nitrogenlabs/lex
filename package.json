{
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "lerna run build",
    "lint": "lerna run lint",
    "clean": "yarn clean:root && lerna run clean",
    "clean:root": "rm -rf *.log node_modules *.lock package-lock.json",
    "test": "jest --clearCache && jest",
    "update": "yarn upgrade-interactive --latest"
  },
  "devDependencies": {
    "@types/jest": "^23.3.9",
    "@types/node": "^10.12.1",
    "jest-cli": "^23.6.0",
    "lerna": "^3.4.3",
    "ts-jest": "^23.10.4",
    "typescript": "^3.1.4"
  },
  "workspaces": [
    "packages/favicons-webpack-plugin",
    "packages/lex",
    "packages/mock-execa",
    "packages/static-site-webpack-plugin"
  ]
}
