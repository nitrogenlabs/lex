{
  "scripts": {
    "build": "lex build -t -r",
    "dev": "lex dev -t",
    "publish": "lex publish",
    "start": "lex dev -o -t"
  },
  "dependencies": {
    "@nlabs/gothamjs": "^0.1.0",
    "react": "^16.4.2",
    "react-dom": "^16.4.2"
  }
}